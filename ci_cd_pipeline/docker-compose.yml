services:
  api:
    build: ./api
    ports:
      - "8000:8000"
    volumes:
      - ./api/app:/app/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  jenkins:
    image: jenkins/jenkins
    environment:
      DOCKER_TLS_CERTDIR: "/certs"
    volumes:
      - jenkins-docker-certs:/certs/client
      - jenkins-data:/var/jenkins_home
    ports:
      - "8080:8080"
      - "50000:50000"
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:8080/login" ]
      interval: 10s
      timeout: 5s
      retries: 10
      start_period: 10s

  jenkins-agent-python:
    build:
      context: ./agent
      dockerfile: Dockerfile-python
    depends_on:
      jenkins:
        condition: service_healthy
    environment:
      JENKINS_URL: "${JENKINS_URL}"
      JENKINS_SECRET: "${PY_JENKINS_SECRET}"
      JENKINS_AGENT_NAME: "${PY_JENKINS_AGENT_NAME}"
      JENKINS_AGENT_WORKDIR: "${JENKINS_AGENT_WORKDIR}"
      JENKINS_WEB_SOCKET: "${JENKINS_WEB_SOCKET}"

  jenkins-agent-node:
    build:
      context: ./agent
      dockerfile: Dockerfile-node
    depends_on:
      jenkins:
        condition: service_healthy
    environment:
      JENKINS_URL: "${JENKINS_URL}"
      JENKINS_SECRET: "${NODE_JENKINS_SECRET}"
      JENKINS_AGENT_NAME: "${NODE_JENKINS_AGENT_NAME}"
      JENKINS_AGENT_WORKDIR: "${JENKINS_AGENT_WORKDIR}"
      JENKINS_WEB_SOCKET: "${JENKINS_WEB_SOCKET}"

  prometheus:
    image: prom/prometheus
    volumes:
      - ./config/prometheus.yml:/etc/prometheus/prometheus.yml
      - prometheus-data:/prometheus
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana
    volumes:
      - grafana-data:/var/lib/grafana
    ports:
      - "3000:3000"

volumes:
  jenkins-docker-certs:
  jenkins-data:
  prometheus-data:
  grafana-data:
